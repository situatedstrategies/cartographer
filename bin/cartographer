#!/usr/bin/env python3
"""Runs the cartographer CLI from this checkout without installing it."""
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.realpath(__file__)))
sys.path.insert(0, ROOT)

from cartographer.cli import main  # noqa: E402

sys.exit(main())
